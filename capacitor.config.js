const { CapacitorConfig } = require('@capacitor/cli');

const config = {
  appId: 'com.lumix.luxefinance',
  appName: 'Luxe Finance',
  webDir: 'www',
  bundledWebRuntime: false
};

module.exports = config;
